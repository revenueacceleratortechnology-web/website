import Link from 'next/link';
import { categoryLinks, dtcLinks } from '@/lib/pages';
export function CategoryDirectory(){return <><section id="categories" className="band bg-mineral"><div className="shell"><p className="eyebrow text-signal">Category expertise</p><h2 className="display mt-4 max-w-2xl text-[length:var(--text-h2)]">Your category. Your customers. A plan that fits.</h2><div className="mt-9 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{categoryLinks.map(l=><Link className="directory-card font-bold" key={l.href} href={l.href}>{l.label}<span className="ml-3 text-signal">↗</span></Link>)}</div></div></section><section id="dtc" className="band bg-paper"><div className="shell grid gap-10 lg:grid-cols-2"><div><p className="eyebrow text-signal">Beyond the marketplace</p><h2 className="display mt-4 text-[length:var(--text-h2)]">Build a stronger direct-to-consumer business.</h2><p className="mt-5 leading-relaxed text-ash">Connect your website, acquisition channels, and customer relationships with a focused plan for growth beyond Amazon.</p><Link className="mt-6 inline-block font-semibold text-signal" href="/dtc">Explore DTC services ↗</Link></div><div className="grid gap-3 sm:grid-cols-2">{dtcLinks.map(l=><Link key={l.href} href={l.href} className="directory-card font-semibold">{l.label} ↗</Link>)}</div></div></section></>}
